version: 2.1
jobs:
  compile:
    docker:
      - image: buildpack-deps:18.04
    steps:
      - checkout
      - run:
          name: compile
          command: |
            ls -la

workflows:
 version: 2
 build:
   jobs:
     - compile

